1. Linux environment
  If you do git clone and enter that folder, enter "pwd" in your command window and replace *** to your result "export CPATH=$CPATH:***/communication/cpp"
2. Windows environment(Visual Studio)
  Just put codes [tcp_protocol_client_windows.h, tcp_protocol_server_windows.h] in header file (in Visual Studio).
  !! This code using C++17 standard compiler, so you change coplier the way which change C++ language standard in project configuration.
