This can installed by "pip install -e" in this folder.
Or if you do git clone and enter that folder, enter "pwd" in your command window and replace *** to your result "pip install -e "***/communication/py".
(above method can run both environment windows and linux)
